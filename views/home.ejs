<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nMeet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <style>
        /* General Styles */
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            background: #1f2124;
            /* Darker Background */
            color: #e0e0e0;
            /* Lighter Text */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Header */
        header {
            padding: 2rem;
            text-align: center;
            background: linear-gradient(to bottom, #242629, #1f2124);
            /* Subtle Gradient */
        }

        .app-title {
            font-size: 2em;
            font-weight: 700;
            color: #00c853;
            /* Green Title */
            margin-bottom: 0.5em;
        }

        /* Main Content */
        main {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        #content-wrapper {
            background: #292b2f;
            /* Darker Container */
            padding: 3rem;
            border-radius: 10px;
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.3);
            text-align: center;
            max-width: 600px;
            /* Sets a maximum width */
            width: 90%;
            /* Takes up 90% of the available width */
        }

        .styled-heading {
            font-size: 1.8em;
            font-weight: 600;
            margin-bottom: 1.5rem;
        }


        /* Form Styling */
        .meeting-container {
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
        }

        .input-container {
            display: flex;
            align-items: center;
            background: #35383c;
            border-radius: 5px;
            padding: 0.8rem 1rem;
        }

        .input-container i {
            margin-right: 10px;
            color: #888;
        }

        .input-box {
            flex: 1;
            background: transparent;
            border: none;
            color: #e0e0e0;
            font-size: 1em;
            padding: 0.5rem;
            outline: none;
        }



        button {
            background-color: #00c853;
            color: #fff;
            padding: 1rem 1.5rem;
            border: none;
            border-radius: 5px;
            font-size: 1em;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #00a846;
        }

        a.start-meeting-btn {
            text-decoration: none;
            display: block;
            /* Make button full width on its own line */
            margin-bottom: 1.2rem;
        }


        /* Footer */
        footer {
            text-align: center;
            padding: 1rem;
            background: #1a1c1f;
            /* Darker footer */
            margin-top: auto;
            font-size: 0.9em;
            color: #888;
        }

        /* Media Query for Responsiveness - Adjust as needed */
        @media (max-width: 500px) {
            .app-title {
                font-size: 2em;
            }

            #content-wrapper {
                padding: 2rem;
            }

            .styled-heading {
                font-size: 1.5em;
            }

            button,
            .input-box {
                font-size: 0.9em;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1 class="app-title">nMeet</h1>
    </header>


    <main>
        <a href="/room" class="start-meeting-btn">
            <button><i class="fas fa-video"></i> Start New Meeting</button>
        </a>
    </main>

    <footer>
        © 2024 nMeet. Privacy Respected
    </footer>
    <script>
        document.getElementById('join-button').addEventListener('click', function () {
            let input = document.getElementById('meeting-link-text-box').value;
            if (input) {
                window.location.assign(input);
            }
            document.getElementById('meeting-link-text-box').value = "";
        });
    </script>
</body>

</html>